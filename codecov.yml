codecov:
  notify:
    after_n_builds: 7

comment:
  after_n_builds: 7
  require_changes: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch: off
